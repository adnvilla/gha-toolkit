{{- /* Migration Job wired as a Helm pre-install/pre-upgrade hook: `helm upgrade` blocks
       until it succeeds and aborts the release if it fails. Default off. */ -}}
{{- if .Values.migrations.enabled -}}
{{- $job := .Values.migrations -}}
{{- $name := $job.name | default "migrations" -}}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "app.job.fullname" (dict "root" . "name" $name "suffix" $job.nameSuffix) }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
    app.kubernetes.io/component: job
  annotations:
    "helm.sh/hook": {{ $job.hook | default "pre-install,pre-upgrade" }}
    "helm.sh/hook-weight": {{ $job.hookWeight | default "-5" | quote }}
    "helm.sh/hook-delete-policy": {{ $job.hookDeletePolicy | default "before-hook-creation" }}
    {{- with $job.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- include "app.job.spec" (dict "job" $job) | nindent 2 }}
  template:
    metadata:
      labels:
        {{- /* Reserved selector keys are stripped from the user map rather than overridden: a
               podLabels entry named app.kubernetes.io/instance would otherwise put this pod behind
               the app Service, and emitting the key twice relies on decoder order. */}}
        {{- with omit ($job.podLabels | default dict) "app.kubernetes.io/name" "app.kubernetes.io/instance" "app.kubernetes.io/component" "app.kubernetes.io/managed-by" }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- include "app.job.podLabels" (dict "root" . "name" $name) | nindent 8 }}
      {{- with $job.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- include "app.job.podSpec" (dict "root" . "job" (merge (dict "name" $name "serviceAccountName" (include "app.migrations.serviceAccountName" .)) $job)) | nindent 6 }}
{{- end }}
