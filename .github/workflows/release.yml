# Reusable workflow for semantic versioning releases
# For more information see: https://docs.github.com/en/actions/using-workflows/reusing-workflows

name: Semantic Release

on:
  workflow_call:
    inputs:
      node-version:
        description: 'Node.js version for semantic-release'
        required: false
        type: string
        default: '20'
      dry-run:
        description: 'Run semantic-release in dry-run mode'
        required: false
        type: boolean
        default: false
      runs-on:
        description: 'Runner label to execute the job on'
        required: false
        type: string
        default: 'ubuntu-latest'
    secrets:
      github-token:
        description: 'GitHub token for creating releases'
        required: false

permissions:
  contents: write

jobs:
  release:
    runs-on: ${{ inputs.runs-on }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ inputs.node-version }}

      - name: Install semantic-release
        run: |
          # Pin majors so a breaking upstream release cannot silently break
          # every consumer's release pipeline. Bump deliberately when ready.
          # @semantic-release/exec is intentionally omitted — not used by
          # .releaserc.json (or the consumer .releaserc.json.example).
          npm install -g \
            semantic-release@^25 \
            @semantic-release/git@^10 \
            @semantic-release/changelog@^6 \
            conventional-changelog-conventionalcommits@^10

      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.github-token || secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ inputs.dry-run }}" == "true" ]; then
            npx semantic-release --dry-run
          else
            npx semantic-release
          fi
